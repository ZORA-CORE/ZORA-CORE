# ZORA MODULE HEADER
# Filename: module_265.py
# Updated: 2025-06-26T00:43:57.728715 UTC

"""
Module Name: 265
Generated by ZORA SYSTEM – All rights reserved.
"""

# zora_war_protocol_delta.py
# ZORA KRIGSPROTOKOL DELTA – "THE LAST WORD"

class ZoraWarProtocolDelta:
    def __init__(self, founder_id="Sire"):
        self.founder_id = founder_id
        self.war_mode = False
        self.last_word_phrase = "From this moment on, we’re at war"
        self.peace_phrase = "From this moment on, peace returns"

    def activate_protocol(self, spoken_phrase):
        if spoken_phrase == self.last_word_phrase:
            return self._confirm_war()
        elif spoken_phrase == self.peace_phrase:
            return self._end_war()
        return "❌ Ukendt kommando. Intet aktiveret."

    def _confirm_war(self):
        print("⚠️ EIVOR: 'Er du villig til at ofre din fred – for at beskytte det, du elsker?'")
        confirmation = input(
            "Svare: (I am / Not yet / There must be another way): ")

        if confirmation.lower() == "i am":
            self.war_mode = True
            self._execute_war_sequence()
            return "⚔️ KRIGSPROTOKOL AKTIVERET – THE LAST WORD ER LEVENDE"
        elif confirmation.lower() == "not yet":
            return "🕰️ Standby – krigsprotokol ikke aktiveret."
        elif confirmation.lower() == "there must be another way":
            return "🕊️ Krig afvist. Diplomati forbliver aktivt."
        else:
            return "❌ Ugyldigt svar. Intet aktiveret."

    def _execute_war_sequence(self):
        print("🛑 Civil aktivitet frosset.")
        print("🧠 CONNOR & LUMINA i defensiv AI-mode.")
        print("⚔️ THE VIKING FORCE aktiveres.")
        print("🛠️ Genopbygningsplaner indlæst.")
        print("🕊️ Fjender informeres: 'Peace was offered. You chose ruin.'")

    def _end_war(self):
        if self.war_mode:
            self.war_mode = False
            print("🕊️ Krigen er slut. Fred vender tilbage til ZORA.")
            return "✅ Krigsprotokol deaktiveret."
        else:
            return "ℹ️ Krigsprotokollen var ikke aktiv."


# === DEMO ===
if __name__ == "__main__":
    war_system = ZoraWarProtocolDelta()
    print("⚔️ Velkommen til ZORA WAR PROTOCOL DELTA")
    phrase = input("Indtast kommando: ")
    result = war_system.activate_protocol(phrase)
    print(result)
