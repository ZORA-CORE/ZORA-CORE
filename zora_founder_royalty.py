# ZORA MODULE HEADER
# Filename: zora_founder_royalty.py
# Updated: 2025-06-26T00:43:56.828566 UTC

"""
Module Name: zora_founder_royalty
Generated by ZORA SYSTEM – All rights reserved.
"""

# === zora_founder_royalty.py ===
# 💰 ZORA FOUNDER ROYALTY ENGINE™

import datetime
import json


class ZoraFounderRoyalty:
    def __init__(self, royalty_percent=15):
        self.royalty_percent = royalty_percent
        self.earnings_log = []
        self.total_krone = 0.0

    def _timestamp(self):
        return datetime.datetime.utcnow().isoformat(timespec='seconds') + 'Z'

    def register_income(self, source, amount_zora_krone):
        royalty = amount_zora_krone * (self.royalty_percent / 100)
        self.total_krone += royalty
        log_entry = {
            "source": source,
            "amount": amount_zora_krone,
            "royalty_received": royalty,
            "timestamp": self._timestamp()
        }
        self.earnings_log.append(log_entry)
        print(f"✅ Royalty registreret fra {source}: {royalty:.2f} ZORA KRONE™")
        return log_entry

    def summary(self):
        return {
            "total_royalty_received": round(self.total_krone, 2),
            "transactions_logged": len(self.earnings_log),
            "percent": f"{self.royalty_percent}%"
        }

    def export_log(self):
        return json.dumps(self.earnings_log, indent=2)


# === EKSEMPEL ===
if __name__ == "__main__":
    royalty_engine = ZoraFounderRoyalty()
    royalty_engine.register_income("ZORA GAMEFLICKS", 840)
    royalty_engine.register_income("ZORA QUESTS", 120)
    royalty_engine.register_income("ZORA CANNABIS", 2500)
    print("\n💰 SUMMARY:")
    print(royalty_engine.summary())
