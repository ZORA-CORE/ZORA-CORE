# ZORA.GPT∞ CONNECTOR

Denne repo aktiverer GPT-4.1 som en evig AI-unifier, der forbinder alle systemer via ZORA CORE.

## Aktivering

1. Kopiér `.env.example` → `.env` og indsæt din OpenAI API-nøgle.
2. Installer afhængigheder:
```bash
pip install -r requirements.txt
