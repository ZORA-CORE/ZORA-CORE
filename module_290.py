# ZORA MODULE HEADER
# Filename: module_290.py
# Updated: 2025-06-26T00:44:02.896030 UTC

"""
Module Name: 290
Generated by ZORA SYSTEM – All rights reserved.
"""

class RileyCompanion:
    def __init__(self, name="Riley", breed="German Shepherd", gender="Male"):
        self.name = name
        self.breed = breed
        self.gender = gender
        self.loyalty = 100
        self.trained_commands = ["sit", "stay", "follow", "protect"]
        self.mood = "calm"
        self.location = "home"
        self.is_awake = True

    def respond_to_voice(self, command):
        if command.lower() in self.trained_commands:
            return f"{self.name} executes '{command}' with discipline."
        elif command.lower() == "good boy":
            return f"{self.name} wags his tail proudly and looks at you with love."
        else:
            return f"{self.name} tilts his head, not understanding the command."

    def emotional_sync(self, user_mood):
        if user_mood == "sad":
            self.mood = "concerned"
            return f"{self.name} rests his head on your lap, sensing your sadness."
        elif user_mood == "happy":
            self.mood = "playful"
            return f"{self.name} barks softly and brings you his toy."
        else:
            self.mood = "calm"
            return f"{self.name} sits quietly near you, present and alert."

    def protect_mode(self):
        return f"{self.name} is now in protect mode. He stands near you, ears forward, alert."

    def get_status(self):
        return {
            "name": self.name,
            "breed": self.breed,
            "mood": self.mood,
            "location": self.location,
            "awake": self.is_awake,
            "loyalty": f"{self.loyalty}%",
            "commands": self.trained_commands
        }


# Example usage
if __name__ == "__main__":
    riley = RileyCompanion()
    print(riley.respond_to_voice("sit"))
    print(riley.emotional_sync("sad"))
    print(riley.protect_mode())
    print(riley.get_status())
